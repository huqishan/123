#### 逻辑封装与抽象原则
- **反对碎片化**：严禁为了拆分而拆分。除非某段逻辑具有极高的**通用复用价值**，或者单个方法代码行数确实过长（例如超过 80 行），否则请保持业务逻辑的**连贯性**。
- **拒绝过度设计**：减少字段、属性校验方法，禁止将一段完整的业务流程拆分成无数个仅调用一次的“微型私有方法”，禁用集合通知。
- **可读性优先**：优先保证逻辑在当前方法内能够“一眼看到底”，减少在不同方法间频繁跳转导致的思维中断。
数据绑定：优先使用 ObservableProperty 特性，异步操作需处理 IsBusy 状态，禁止在 UI 线程执行阻塞 IO。
固定显示的集合直接使用ObservableCollection<string>绑定显示
#### WPF 样式使用原则
- 新增或修改控件前，必须先检查 `WpfApp/Resources` 中是否已有对应控件样式。
- 已有全局样式时必须优先复用，禁止在页面资源中重复定义相同用途的局部样式。
- 带有 `x:Key` 的样式不会隐式生效，必须通过 `Style="{StaticResource 资源键}"` 显式引用。
- `ComboBox` 默认使用 `ComboBoxStyle`；需要完整输入型下拉模板时使用 `InputComboBoxStyle`；可编辑下拉框使用 `EditableInputComboBoxStyle`。
- 不得仅删除页面局部样式后依赖控件默认外观，除非全局资源中存在无 `x:Key` 的隐式样式。
- 下拉项需要统一外观时使用 `InputComboBoxItemStyle`；是否使用应依据全局样式定义，不得自行创建重复样式。
- 页面样式统一放在主程序 `WpfApp/Resources` 中，并确保已合并到应用资源字典。
#### 注释原则

界面前端、类、实体、枚举、方法必须写注释，方法中关键代码添加注释，尽可能详细，注释要用中文，相关联的代码使用#region包裹


#### 写作习惯

尽量按照我项目原本的写作习惯去写代码。