# Domain Docs

This repository uses a single-context domain documentation layout.

Before exploring a domain area, read `CONTEXT.md` at the repository root and relevant ADRs under `docs/adr/` when they exist. If either location is absent, continue silently.

Use the domain vocabulary defined by `CONTEXT.md`. Surface any proposed change that conflicts with an existing ADR instead of silently overriding it.
